---
'praetorium.gg': patch
---

Stop blocking a scoring selection once the round or battle VP cap is reached — it now stays a warning, and only the overage is left uncounted, so the rest of that turn's legitimate scoring can still go through.
